version: '3.8'

services:
  superiorpractices:
    build: .
    container_name: superiorpractices-website
    restart: unless-stopped
    ports:
      - "3003:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
